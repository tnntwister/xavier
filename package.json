{
  "name": "xavier",
  "version": "0.1.0",
  "description": "a node tool to convert vcard files into printable PDFs",
  "main": "main.js",
  "scripts": {
    "test": "jest",
    "watch": "watchify main.js -o bundle.js -t [ babelify --presets [ env ] ] -d -v"
  },
  "author": "François-Xavier Guillois <fxguillois@lilo.org>",
  "license": "ISC",
  "dependencies": {
    "pdfmake": "^0.1.33",
    "vcard-js": "^1.2.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "jest": "^21.2.1"
  }
}
